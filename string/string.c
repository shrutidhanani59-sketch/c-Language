#include<stdio.h>
#include<conio.h>
#include<string.h>

int main()
{
    char letter[]="hello";
    char name[5];
    // printf(" %c", letter[0]);
    // printf(" %s", letter);
    // printf("%s",strrev(letter));
    // strcpy(name,"shruti");
    int length=letter;
    printf("%d", strlen(length));
    // printf("%s", strlwr(letter));
    // printf("%s", strupr(letter));

    // for(int i=0; letter[i]!=NULL; i++)
    // {
    //    printf("%c", letter[i]);
    // }
}
